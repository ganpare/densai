# 環境変数のサンプルファイル
# 実際に使用する場合は、このファイルを .env にコピーして値を設定してください
# cp .env.example .env

# データベース接続URL
# Docker環境では docker-compose.yml で自動設定されます
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/bond_inquiry_db

# セッション暗号化キー
# 本番環境では必ず強力なランダム文字列に設定してください
# 生成方法: openssl rand -base64 32
# 注意：この値は必須です。空のままにしないでください。
SESSION_SECRET=CHANGE_ME_GENERATE_SECURE_RANDOM_STRING

# アプリケーション設定
NODE_ENV=production
PORT=5000

# PostgreSQL設定（docker-compose.yml内で使用）

